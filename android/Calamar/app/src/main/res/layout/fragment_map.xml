<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="ch.epfl.sweng.calamar.map.MapFragment">

    <fragment
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MapsActivity"
        tools:layout="@layout/abc_screen_simple" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/create_item_button_map"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="right|bottom"
        android:layout_marginBottom="12dp"
        android:layout_marginEnd="12dp"
        android:layout_marginLeft="0dp"
        android:layout_marginRight="12dp"
        android:layout_marginStart="0dp"
        android:layout_marginTop="0dp"
        android:clickable="true"
        android:scaleType="center"
        android:src="@drawable/ic_add_circle_outline_black_48dp"
        app:backgroundTint="@android:color/white"
        app:borderWidth="0dp"
        app:fabSize="normal"
        app:layout_anchor="@id/recipientLabel"
        app:layout_anchorGravity="bottom|right|end" />

</FrameLayout>
